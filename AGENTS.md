# AGENTS.md

## 项目状态

- 项目名：一次买够（It's Mygo）。
- 类型：社交新零售电商平台。
- Python 环境：3.12.10。
- 当前阶段：全功能基线已完成，项目基本完结，后续主要是前端交互、展示效果、答辩演示和少量体验优化。
- 默认数据库：MySQL 8.x。SQLite 仅作为早期历史文件保留，不再作为默认开发库。
- 支付方式：支付宝沙箱扫码支付。
- AI：用户端悬浮 AI 购物助手最小版已接入 qwen-flash，当前不读取实时业务数据。

这是学生实训项目，不完全追求生产级复杂度，但必须保持结构清晰、可维护、方便三人协作和答辩讲解。

## 文档优先级

后续接手或上下文压缩后，优先阅读：

1. `AGENTS.md`：项目状态、协作规则、关键业务约定。
2. `docs/final-requirements.md`：按当前实现整理的最终版需求说明。
3. `docs/final-design.md`：按当前实现整理的最终版实现设计说明。
4. `docs/dev-setup.md`：本地启动、MySQL、账号、清库和人工验收流程。
5. `docs/api/*.md`：接口契约，字段、权限、状态、错误码以这里为准。
6. `docs/project-summary.md` 和 `docs/defense-guide.md`：报告、PPT、讲解视频准备材料。

根目录 `需求规格说明书.md` 和 `实现设计说明书.md` 是早期历史资料，不再继续维护，也不再作为当前版本主要依据。如与当前代码或接口文档冲突，以当前实现、接口文档和 `docs/final-*.md` 为准。

编码时必须同步文档。接口字段、状态、错误码、权限边界、价格规则、页面入口变化时，同轮更新相关文档。

## 当前功能基线

- 普通用户、平台运营、商家运营账号体系分离，普通用户和后台管理员不能混用。
- 管理端支持平台账号和商家账号同时登录，平台端与商家端 token 分开存储。
- 商家自助注册入驻：`merchant_pending` 可登录但不能管理商品；平台审核通过后创建店铺并升级为 `merchant_operator`。
- 平台不手动创建店铺或商品。店铺来自入驻审核通过，商品由商家创建；平台负责分类、首页轮播、入驻审核、商品监管和内容治理。
- 分类支持最多三级、父级筛选包含子孙分类。用户端首页使用紧凑胶囊筛选条，只展示分类名称，选中父分类后才展开下一级。
- 首页轮播图已接入：平台端 `/admin/home-banners` 可上传图片、设置标题/副标题、排序、启停和跳转商品或链接；用户端首页通过 `/home/banners` 展示启用轮播图。
- 商品支持多图、图文详情、SKU、库存、销售价、划线价、销量、商品收藏、店铺关注、店铺主页。
- 商家端可维护店铺名称、Logo、公告、商品、SKU、库存、优惠券、满减、拼团、订单发货、售后和本店客服。
- 购物车支持按店铺分组、商品勾选、店铺勾选、数量修改、移除、批量删除、清空和失效商品提示。
- 跨店下单会按店铺拆成多张订单并共用一个支付单。
- 支付链路采用支付宝沙箱扫码支付。沙箱网关使用 `https://openapi-sandbox.dl.alipaydev.com/gateway.do`。
- 发货与收货覆盖商家选择物流公司、填写或生成模拟单号、用户确认收货等流程。
- 评价按订单明细商品分别评价，支持星级、文字和图片，发布后默认公开。
- 售后按订单明细和数量处理：买 2 件可退 1 件，但单件商品按单件全额退款；不实现取消售后。
- 促销支持优惠券、满减、积分抵扣、积分流水和会员积分配置。
- 社区支持分区、发帖、评论、点赞、收藏、话题、用户主页、关联商品卡片、平台隐藏治理。
- 种草帖必须绑定已购商品。用户从种草帖进入商品详情并加购后，购物车保存 `source_post_id` 并展示种草来源；确认收货后给种草推广人和下单者分别奖励命中商品原价 1% 积分。
- 拼团基础链路已接入：商家为本店 SKU 创建 2 人或 3 人团，用户直接开团/参团并支付宝支付，成团后订单进入待发货。
- 客服系统已接入：用户可联系平台客服或商家客服，支持会话管理、消息收发和 WebSocket 基础推送。新会话会自动发送一次欢迎语。
- AI 购物助手最小版已接入：用户端非登录页右侧悬浮展示，调用 `/api/v1/ai-assistant/chat`。后端优先调用 qwen-flash 的 OpenAI 兼容接口；配置 `DASHSCOPE_API_KEY` 或 `AI_ASSISTANT_API_KEY` 后启用，未配置或请求失败时降级为项目内预设回复。

## 促销、积分与价格规则

- 平台促销范围：全平台、分类、商家、商品、SKU。
- 商家促销范围：全店铺、本店商品、本店 SKU。
- 内部 `scope_type` 使用 `all/platform/merchant/category/product/sku`，其中 `platform` 兼容旧数据。
- 商家后台只列出和管理 `owner_merchant_id` 等于当前店铺的促销。
- 一个普通订单只能选择一种满减和一张优惠券。
- 满减是“每满多减”，例如每满 100 减 10，适用金额 260 元时减 20 元。
- 价格计算顺序：商品价或活动价 -> 满减 -> 优惠券 -> 积分抵扣 -> 支付宝支付。
- 优惠券门槛按满减后的适用范围金额计算。
- 跨店订单中，平台券/平台满减按适用商品金额分摊到各店铺订单；店铺、商品、SKU 范围只分摊到适用订单。
- 积分类似账户余额，是支付前的金额扣除渠道，可与任何优惠叠加，但受平台配置的单笔抵扣上限限制。
- 会员等级配置支持 `sign_in_bonus_points`、`max_points_discount_percent`、`points_multiplier` 和 `benefit_description`。其中签到加成和等级积分抵扣上限已参与实际计算；`points_multiplier` 当前用于权益展示和后续积分发放扩展预留。
- 积分变动必须通过 `points_service.change_points`，不要直接改 `user.points`。
- 拼团订单使用独立拼团价，不叠加满减或优惠券，不参与社区种草；积分可用于拼团支付前抵扣，仍受平台配置上限限制。
- 拼团允许一次购买多件同一 SKU；库存、订单金额和积分抵扣按件数计算，成团人数按已支付用户数计算。

## 前端约定

- 用户端已从测试控制台拆为正式页面。
- 用户端路由：`/` 首页、`/login` 登录、`/register` 注册、`/products/:productId` 商品详情、`/group-buy` 拼团、`/cart` 购物车、`/checkout` 结算、`/orders` 订单列表、`/orders/:orderId` 订单详情、`/community` 社区、`/customer-service` 客服兼容页、`/user` 用户中心、`/merchants/:merchantId` 店铺主页。
- 公共浏览页允许游客访问；购物车、结算、订单、用户中心等私有页要求登录；公共页上的私有操作在点击时提示登录并跳转。
- 管理端路由：平台端 `/admin/*`，商家端 `/merchant/*`，商家入驻 `/onboarding`。
- 普通用户、平台运营、商家运营不要混在一个页面。
- 前端页面必须清晰、可用、基础美观，不能只靠接口返回查看业务结果。
- 后端新增功能时，前端必须同轮添加可发现的菜单、按钮、表单、列表或页面入口。
- 页面展示给用户的状态必须使用中文文案，不要直接暴露 `pending/group_pending/success/expired` 等枚举。
- 有 ID 或需要通过 ID 操作的内容，必须在业务区直接展示 ID；面向普通用户的非必要 ID 不展示。
- 需要批量输入 ID 或标签的表单，应统一兼容中文逗号、英文逗号、分号和空白分隔；能用选择器的商品选择不要要求手输商品 ID。
- 用户端公共工具函数提取到 `user-frontend/src/utils/format.ts`，新页面应复用，不要重复定义金额、图片、状态等格式化逻辑。

## 当前业务范围说明

- 物流能力覆盖商家发货、快递公司、快递单号和用户确认收货。
- 售后能力覆盖按订单明细和数量申请、商家/平台查看与处理。
- 商品标签用于秒杀、满减、热销、新品等营销场景。
- 支付能力覆盖支付宝沙箱扫码支付，前端正常业务以支付宝支付链路为准。
- AI 助手当前用于购物咨询、流程解释和客服回复草稿方向的扩展，不参与关键业务状态自动变更。

## 后续维护重点

1. 前端交互细节：继续优化页面空状态、错误提示、加载态、表单体验、订单/客服/社区可读性。
2. 答辩演示数据：准备平台分类、首页轮播、商家、商品、促销、拼团、社区、订单、售后、报表等完整演示数据。
3. AI 助手增强：可逐步接入公开商品、店铺、促销、订单状态等只读数据，也可为平台/商家客服提供建议回复。
4. 测试补强：重点覆盖支付、跨店订单、促销分摊、售后、拼团、客服权限和社区种草奖励。
5. 文档维护：任何接口、状态、权限、价格规则、页面入口变化都要同步 `docs/api/*.md`、`docs/dev-setup.md` 和本文件。

## 接口约定

统一响应：

```json
{ "code": 0, "message": "ok", "data": {} }
```

分页响应：

```json
{ "list": [], "page": 1, "page_size": 20, "total": 0 }
```

常用错误码：

- `40001`：参数错误。
- `40002`：未登录或 token 无效。
- `40003`：无权限或越权访问。
- `40004`：数据不存在。
- `40005`：业务冲突或当前资源不可用。
- `40008`：状态不允许执行当前操作。

## 测试和验证

后端改动后优先运行：

```powershell
.\.venv\Scripts\python.exe -m pytest backend\tests -q
.\.venv\Scripts\python.exe -m compileall backend\app backend\tests
```

前端改动后运行：

```powershell
cd user-frontend
.\node_modules\.bin\tsc.cmd -b

cd admin-frontend
.\node_modules\.bin\tsc.cmd -b
```

如需完整构建：

```powershell
npm run build
```

## 数据库和迁移注意

- 当前开发和答辩环境默认使用 MySQL，连接串配置在根目录 `.env` 的 `DATABASE_URL`。
- MySQL 数据库本身用 `backend/scripts/init_mysql_db.py` 创建；SQLAlchemy `create_all` 只负责在已有库中建表。
- 原 SQLite 数据不会自动迁移到 MySQL；平台管理员、分类、商家、商品、订单等数据都需要重新创建或重新录入。
- 新库首次启动只自动建表，不自动写入分类。分类属于平台运营配置，由平台后台自行创建。
- SQLite 补列逻辑仅作为历史兼容保留，不再作为默认开发路径；新增字段优先考虑 MySQL 新库建表和后续 Alembic 迁移方案。
- 如果必须改已有表字段，要说明本地开发库如何处理，必要时提供迁移或清库建议。

## Git 和协作

- 不要直接推送代码到 GitHub；由项目负责人自行上传或覆盖远端代码。
- 提交前先看 `git status`，不要误改或回滚他人工作。
- 多人协作时，目录、命名、接口和文档要保持统一。
- 新增文件命名要清晰，避免缩写过度。
- 如发现已有改动和当前任务冲突，先说明冲突，不要擅自覆盖。

## AI 编码注意事项

- 每次编码前先读相关现有代码和文档。
- 发现早期需求/设计文档和当前代码明显不一致时，以当前实现、接口文档和最终版文档为准。
- 编码后同步文档和测试。
- 不要随意引入不必要的新依赖。
- 不要把业务结果只放进接口返回排查区。
- 完成回复要说明改了哪些文件、验证了什么、还有哪些后续风险。
