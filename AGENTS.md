# AGENTS.md

## 最新进度：全功能基线已完成，AI 助手最小版已接入

- 项目名：一次买够（It's Mygo）。
- 类型：社交新零售电商平台。
- Python 环境：3.12.10。
- 当前状态：用户端、平台端、商家端主要功能均已前后端完整实现；AI 购物助手最小版已接入，可作为协作交付基线。
- 后续重点：AI 助手数据读取与客服建议回复、订单详情继续深化、客服实时体验增强、报表细节优化和文档维护。
- 最近说明：首页轮播图已完成后端接口、平台端管理页和用户端首页展示；上传文件写入和静态访问目录已统一到 `backend/uploads`；用户端首页分类已改为紧凑胶囊筛选，选中父级后才逐级展开子分类且不展示分类 ID。订单详情已补齐评价商品和申请售后入口；此前已优化客服入口和客服工作台 UI。平台/商家报表以后端已有指标为基础补充了成交趋势、状态分布、售后玫瑰图、促销漏斗、排行和社区互动雷达等可视化。会员积分配置已支持等级签到加成、等级积分抵扣上限、积分倍率预留值和权益说明。需求规格说明书和实现设计说明书已降级为历史资料，后续不再作为主要编码依据。

这是学生实训项目，不完全追求生产级，但必须结构清晰、可维护、方便三人协作。

## 文档优先级

- `docs/handoff.md` 是移交入口文档，接手时优先阅读。
- `docs/api/*.md` 是接口契约，字段、权限、状态、错误码以这里为准。
- `docs/development-roadmap.md` 是后续任务路线图。
- `AGENTS.md` 是 AI 协作和上下文恢复文档，需要随阶段更新。
- `需求规格说明书.md` 和 `实现设计说明书.md` 是早期历史资料，后续不再优先参考；如与当前代码或接口文档冲突，以当前实现、接口文档和移交文档为准。

编码时必须同步文档。接口字段、状态、错误码、权限边界、价格规则、页面入口变化时，同轮更新相关文档。

## 当前功能基线

- 用户认证与管理端认证分离，普通用户和后台管理员不能混用。
- 管理端支持平台账号和商家账号同时登录，平台端与商家端 token 分开存储。
- 商家自助注册入驻：`merchant_pending` 只能查看和重新提交入驻资料；平台审核通过后创建店铺并升级为 `merchant_operator`。
- 平台不手动创建店铺或商品。店铺来自入驻审核通过，商品由商家创建；平台负责分类、入驻审核、商品监管和内容治理。
- 分类支持最多三级、父级筛选包含子孙分类、平台编辑和软停用；用户端首页使用紧凑分类筛选条，只展示分类名称，选中父分类后才展开下一级子分类，没有子分类则不显示下级区域。
- 首页轮播图已接入：平台端 `/admin/home-banners` 可上传图片、设置标题/副标题、排序、启停和跳转商品或链接；跳转商品通过可搜索商品下拉框选择，不再手填商品 ID；用户端首页通过 `/home/banners` 展示启用轮播图，并提供左右切换按钮。上传图片由 `/upload/image` 写入 `backend/uploads`，静态访问也挂载同一目录。
- 商品支持多图、图文详情、SKU、库存、销售价/划线价、商品收藏、店铺关注、店铺主页。商品详情页展示 `detail_images` 详情图和 `sales_count` 销量。
- 商家端可维护店铺名称、Logo、公告、商品基础信息、图片、SKU、库存、优惠券、满减、拼团和售后。
- 购物车支持数量修改、移除、勾选、批量删除、清空和失效商品提示。
- 跨店下单会按店铺拆成多张订单并共用一个支付单。
- 支付只接入支付宝沙箱扫码支付；正常前端业务不使用 mock 支付。沙箱网关使用 `https://openapi-sandbox.dl.alipaydev.com/gateway.do`。
- 发货与收货只做商家在订单详情中选择物流公司、填写或生成模拟单号，用户确认收货，不实现真实物流轨迹查询。
- 评价按订单明细商品分别评价，支持星级和图片，发布后默认公开。
- 售后按订单明细和数量处理：买 2 件可退 1 件，但单件商品按单件全额退款；不实现取消售后。
- 社区支持发帖、评论、点赞、收藏、平台隐藏管理；帖子和评论发布后默认公开。帖子详情 Modal 中关联商品卡片可点击跳转商品详情页，URL 携带种草来源参数；商品详情页加购时自动携带 `source_post_id`，购物车展示种草来源标签；已移除独立的"种草来源下单"弹窗。拼团商品点击后跳转商品详情页，URL 携带 `group_buy_activity_id` 参数，详情页检测到拼团参数时展示拼团价（红色大字）+ 划线原价 + 节省金额，操作按钮变为「发起拼团」。用户可收藏/取消收藏帖子，可删除自己的帖子；用户中心展示收藏帖子列表。
- 社区分区支持 `square/grass/merchant/help/experience`；`square` 是综合广场并展示全部公开帖子，`grass` 是种草专区。
- 社区基础话题已实现：帖子 `topic_tags` 可由前端输入，热门话题接口可返回话题，帖子列表支持话题筛选。
- 社区用户主页已实现：可查看作者摘要、帖子数、种草帖数、评论数、获赞数和近期帖子。
- 种草帖必须绑定已购商品，普通帖和商家动态可绑定商品但不触发种草奖励。
- 用户端发帖已使用“搜索商品 + 商品卡片勾选 + 已选商品标签”的关联商品选择方式；商家端发帖仍为可搜索商品选择器。用户端、平台端、商家端帖子详情均应展示关联商品卡片。
- 商家端可浏览社区完整内容并发布商家动态；商家动态由后端强制为商家类型，避免混用普通用户种草。
- 平台端社区管理应展示帖子全文、图片、关联商品、评论，并提供隐藏帖子和隐藏评论能力。
- 种草来源加购会在购物车保存 `source_post_id` 并展示来源，确认收货后给种草推广人和下单者分别奖励命中商品原价 1% 积分。
- 拼团基础链路已接入：商家为本店 SKU 创建 2 人或 3 人团，用户直接开团/参团并支付宝支付，已支付待成团订单为 `group_pending`，成团后进入商家待发货。
- 客服系统已接入：用户可联系平台客服或商家客服，支持会话管理、消息收发和 WebSocket 实时推送。用户首次创建会话时，后端会自动发送一次客服欢迎语：商家客服使用当前店名，平台客服使用“一次买够”平台名；暂不提供自动回复编辑。用户中心已内嵌客服消息板块，兼容路由 `/customer-service` 仍保留完整客服页；商品详情页已实现右下角商家客服弹窗；订单列表不直接放联系客服入口，订单详情页提供“联系商家客服”和“联系平台客服”两个弹窗入口。平台端和商家端客服会话为正式双栏工作台，状态展示使用“进行中/已结束”，结束操作文案使用“结束”。
- AI 购物助手最小版已接入：用户端非登录页右侧悬浮展示，调用 `/api/v1/ai-assistant/chat`。后端优先调用 qwen-flash 的 OpenAI 兼容接口；配置 `DASHSCOPE_API_KEY` 或 `AI_ASSISTANT_API_KEY` 后启用，未配置或请求失败时降级为项目内预设回复。当前不读取实时业务数据。

## 促销、积分与价格规则

- 平台促销范围：全平台、分类、商家、商品、SKU。
- 商家促销范围：全店铺、本店商品、本店 SKU。
- 内部 `scope_type` 使用 `all/platform/merchant/category/product/sku`，其中 `platform` 兼容旧数据。
- 促销创建归属使用 `owner_merchant_id` 和 `created_by_admin_id` 记录。
- 商家管理促销必须后端校验归属，不能只靠前端过滤。
- 商家后台只列出和管理 `owner_merchant_id` 等于当前店铺的促销。平台创建并投放到该店的优惠券/满减可以给用户领取和结算使用，但商家后台不能看到、编辑或停用。
- 一个普通订单只能选择一种满减和一张优惠券。
- 满减是“每满多减”，例如每满 100 减 10，适用金额 260 元时减 20 元。
- 价格计算顺序：商品价或后续活动价 -> 满减 -> 优惠券 -> 积分抵扣 -> 支付宝支付。
- 优惠券门槛按满减后的适用范围金额计算。
- 跨店订单中，平台券/平台满减按适用商品金额分摊到各店铺订单；店铺/商品/SKU 范围只分摊到适用订单。
- 积分类似账户余额，是支付前的金额扣除渠道，可与任何优惠叠加，但受平台配置的单笔抵扣上限限制。
- 会员等级配置支持 `sign_in_bonus_points`、`max_points_discount_percent`、`points_multiplier` 和 `benefit_description`。其中签到加成和等级积分抵扣上限已参与实际计算；`points_multiplier` 当前用于会员权益展示和后续积分发放扩展预留，不改变现有种草奖励固定 1% 规则。
- 积分变动必须通过 `points_service.change_points`，不要直接改 `user.points`。
- 拼团订单使用独立拼团价，不叠加满减或优惠券，不参与社区种草；积分可用于拼团支付前抵扣，仍受平台配置上限限制。
- 拼团允许一次购买多件同一 SKU；库存、订单金额和积分抵扣按件数计算，成团人数按已支付用户数计算，不按件数计算。
- 拼团首位用户支付后团有效期 24 小时；达到活动成团人数且参与者均支付后，团变为 `success`，订单由 `group_pending` 进入 `pending_shipment`。

## 前端约定

- 用户端已从单体测试控制台拆为独立页面，不再是联调工作台。
- 用户端路由：`/` 首页（直接渲染商品列表页）、`/login` 登录、`/register` 注册、`/products`（已重定向至 `/`）、`/products/:productId` 商品详情、`/group-buy` 拼团、`/cart` 购物车、`/checkout` 结算、`/orders` 订单列表、`/orders/:orderId` 订单详情、`/community` 社区、`/customer-service` 客服消息兼容页、`/promotions` 优惠券（已重定向至 `/user`）、`/addresses` 地址（已重定向至 `/user`）、`/user` 用户中心、`/merchants/:merchantId` 店铺主页。公共浏览页应允许游客访问；购物车、结算、订单、用户中心等私有页继续要求登录；公共页上的私有操作在点击时提示登录并跳转。
- 顶部导航栏右侧登录态展示「头像 + 昵称」胶囊按钮（点击进入个人中心），无头像时显示昵称首字母；导航链接：首页、拼团专区、购物车、订单、社区。
- 用户端公共工具函数提取到 `user-frontend/src/utils/format.ts`，所有页面共用，新页面应从 `../../utils` 导入而非重复定义。
- 原测试控制台代码保留在 `user-frontend/src/pages/test-console/index.tsx` 作为功能完整性参考，不再被路由引用，新功能不要回写到测试控制台。
- 管理端已拆为平台后台和商家后台正式路由：平台端 `/admin/*`，商家端 `/merchant/*`，商家入驻 `/onboarding`。
- 普通用户、平台运营、商家运营不要混在一个页面。
- 前端页面必须清晰、可用、基础美观，不能只靠接口返回查看业务结果。
- 后端新增功能时，前端必须同轮添加可发现的菜单、按钮、表单、列表或页面入口。
- 购物车/结算区必须展示商品合计、满减、优惠券、积分抵扣、总抵扣、支付宝应付金额和可选促销。
- 用户端页面各司其责（2026-07-04 起）：
  - 首页 `/`：顶部先展示“发现好物”和搜索框，下方展示平台首页轮播图、紧凑分类筛选条、价格/排序筛选、商品卡片和分页。分类筛选条先展示一级分类；选中父分类后才展开其直接子分类，继续选中子分类后再展开下一层；用户端不展示分类 ID。点击父级分类时后端返回其全部子孙分类商品。
  - 拼团专区页 `/group-buy`：只列出活动和可加入的团，点击商品卡片跳转 `/products/<productId>?group_buy_activity_id=<activityId>`（显示拼团价+划线原价），点击「发起拼团」跳转 `/checkout?group_buy=<activityId>`，点击「加入此团」跳转 `/checkout?group_join=<groupId>`，不再在拼团页选地址/件数/积分或显示支付二维码。
  - 购物车页 `/cart`：只管理商品（勾选/数量/删除/清失效），点击「去结算」跳转 `/checkout`。
  - 结算页 `/checkout`：统一处理收货地址、购买件数、满减、优惠券、积分，兼容三种入口——购物车（默认）、发起拼团（`?group_buy=<activityId>`）、加入拼团（`?group_join=<groupId>`）。满减/优惠券/积分选择变更时自动重新计算金额。拼团模式不展示满减/优惠券选项，提交时调用 `groupBuyService.startGroup`/`joinGroup`；购物车模式调用 `orderService.createOrder`。提交后留在结算页自动生成支付宝二维码，支付同步成功后跳转订单页。收货地址区域提供「新增收货地址」按钮，可弹窗新建地址（含省市区下拉）并自动选中。
  - 订单页 `/orders`：订单列表、状态筛选、分页和快捷操作；“查看详情”带图标并进入 `/orders/:orderId`；客服入口、支付信息和售后记录不放在列表页中。
  - 订单详情 `/orders/:orderId`：展示订单流转、商品图片与明细、收货地址、物流公司/单号、优惠与积分抵扣、支付单信息；未支付订单可继续生成/刷新支付宝二维码；已完成订单可评价具体订单明细商品，发货/已收货/已完成订单可按订单明细和数量申请售后；提供商家客服和平台客服两个右下角弹窗入口，直接进入具体订单会话。
  - 用户中心 `/user`：左侧导航包含积分会员、优惠券、地址、商品收藏、收藏帖子、关注店铺和客服消息；客服消息复用完整客服面板。
- 有 ID 或需要通过 ID 操作的内容，必须在业务区直接展示 ID。
- 需要批量输入 ID 或标签的表单，应统一兼容中文逗号、英文逗号、分号和空白分隔；能用选择器的商品选择不要要求手输商品 ID。
- 收货地址表单的省/市/区使用 `region-data.ts` 内置数据的级联 Select 下拉，街道仍为手动输入；手机号字段均有正则验证 `/^1[3-9]\d{9}$/`。
- 商品列表排序支持最新上架、价格升序、价格降序、销量优先；筛选按钮为重置按钮（清空所有条件恢复默认）。
- 优惠券卡片展示使用说明：门槛、适用范围、有效期、限领数量。
- 管理端页面已按平台后台和商家后台拆分，后续只做细节优化和指标扩展。
- 报表图表中订单、售后、拼团等状态必须展示中文文案；不要直接把 `pending/group_pending/success/expired` 等枚举裸露给用户。

## 不做或暂不做

- 不实现真实物流轨迹查询。
- 不实现售后取消。
- 不做商品迁移功能。
- 不做品牌体系；商品标签只做秒杀、满减、热销、新品等营销用途。
- 不接入微信支付。
- 不把 mock 支付暴露给正常前端业务。

## 后续优先任务

1. 公共浏览与商城主页：游客可浏览首页、商品详情、店铺、拼团和社区；首页轮播图和逐级展开分类筛选已实现，后续可继续优化推荐算法和展示素材。
2. 订单详情继续深化：已具备 `/orders/:orderId` 详情页，含支付、确认收货、评价、售后申请、客服弹窗和基础物流/优惠展示；后续可继续补售后详情聚合、退款联动说明、商家信息展示和未支付订单与结算页支付流程复用。
3. 结算支付体验：结算页已能提交后自动生成支付宝二维码并同步结果；后续继续排查沙箱卡顿、超时提示和重复预创建边界。
4. 客服体验：商品详情和订单详情客服弹窗已具备基础可用性；用户中心已内嵌客服消息；平台端和商家端客服工作台已清理开发提示。后续可补 WebSocket 实时接收、图片消息和更多业务化提示。
5. 报表与会员配置：报表后端保持小步扩展，优先丰富前端可视化；会员配置后续可继续把积分倍率接入订单返积分、活动返积分等新增发放场景。
6. AI 助手：用户端悬浮购物助手最小版已完成并接入 qwen-flash；后续可继续做业务数据读取和平台/商家客服建议回复。AI 只做解释、推荐和草稿，不直接修改订单、库存、退款、积分等关键状态。
7. 文档维护：任何接口、状态、权限、价格规则、页面入口变化都要同步 `docs/api/*.md`、`docs/dev-setup.md`、`docs/handoff.md` 和本文件。

## 接口约定

统一响应：

```json
{ "code": 0, "message": "ok", "data": {} }
```

分页响应：

```json
{ "list": [], "page": 1, "page_size": 20, "total": 0 }
```

常用错误码：

- `40001`：参数错误。
- `40002`：未登录或 token 无效。
- `40003`：无权限或越权访问。
- `40004`：数据不存在。
- `40005`：业务冲突或当前资源不可用。
- `40008`：状态不允许执行当前操作。

## 测试和验证

后端改动后优先运行：

```powershell
.\.venv\Scripts\python.exe -m pytest backend\tests -q
.\.venv\Scripts\python.exe -m compileall backend\app backend\tests
```

前端改动后运行：

```powershell
cd user-frontend
.\node_modules\.bin\tsc.cmd -b

cd admin-frontend
.\node_modules\.bin\tsc.cmd -b
```

如果 Codex 沙箱因 Vite/esbuild 权限问题阻止完整构建，可先运行 TypeScript 检查，并在最终回复说明限制。

## 数据库和迁移注意

- 当前开发环境主要依赖 ORM `create_all` 建表。
- 给已有表新增字段时，要同步 `backend/app/db/session.py` 的 SQLite 补列逻辑。
- 如果必须改已有表字段，要说明本地开发库如何处理，必要时提供迁移或清库建议。

## Git 和协作

- 不要直接推送代码到 GitHub；由项目负责人自行上传或覆盖远端代码。
- 提交前先看 `git status`，不要误改或回滚他人工作。
- 多人协作时，目录、命名、接口和文档要保持统一。
- 新增文件命名要清晰，避免缩写过度。
- 如发现已有改动和当前任务冲突，先说明冲突，不要擅自覆盖。

## AI 编码注意事项

- 每次编码前先读相关现有代码和文档。
- 发现早期需求/设计文档和当前代码明显不一致时，以当前实现和接口文档为准；必要时修订移交文档或接口文档，不再优先修改早期需求书/实现书。
- 编码后同步文档和测试。
- 不要随意引入不必要的新依赖。
- 不要把业务结果只放进接口返回排查区。
- 完成回复要说明改了哪些文件、验证了什么、还有哪些后续风险。
