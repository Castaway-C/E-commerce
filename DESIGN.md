# 一次买够前端视觉与交互说明

本文记录当前项目前端的基础视觉方向和后续交互优化原则。它不是严格设计系统，而是为了让后续同学在继续微调页面时保持风格统一。

## 设计目标

一次买够是社交新零售电商平台，页面需要兼顾“商城感”和“后台管理效率”：

- 用户端要像可正常浏览和购买的商城，而不是接口测试页。
- 管理端要像运营后台，信息密度适中，表格、筛选、状态和操作清晰。
- 社区和客服要有真实使用场景感，避免出现开发测试术语。
- 当前主要面向 PC 端展示，移动端可以作为后续扩展。

## 色彩

当前主视觉采用深色导航、浅灰页面背景和紫色品牌按钮：

- 页面背景：`#F8FAFC`。
- 主文字和导航深色：`#1E293B`。
- 品牌强调色：`#6366F1`。
- 卡片背景：`#FFFFFF`。
- 辅助文字：`#64748B`。
- 成功、警告、错误状态使用 Ant Design 语义色或接近语义的 Tag/Badge 颜色。

页面避免大面积纯白直铺；主要内容使用白色 Card 承载，配合圆角和轻阴影形成层次。

## 布局原则

- 页面主容器保留足够边距，PC 端通常使用 24px 或更大内边距。
- 商品详情采用左图右文布局：左侧图片与预览，右侧价格、SKU、库存、店铺和操作。
- 购物车按店铺分组，支持店铺勾选和商品勾选，商家信息应明显展示。
- 结算页突出地址、商品、优惠、积分和支付区，支付二维码不应被挤到页面很深处。
- 订单列表展示摘要，复杂信息进入订单详情页。
- 社区列表使用卡片或网格展示，帖子详情展示内容、图片、关联商品和评论。
- 平台/商家后台采用“查询/操作区 + 表格/列表 + 分页/详情弹窗”的常规管理布局。

## 组件使用

- 基础组件优先使用 Ant Design 5。
- 状态展示使用 `Tag`、`Badge`、`Steps`、`Timeline` 等语义化组件。
- 弹窗使用 `Modal` 或抽屉，不把简单对话强制跳转成整页。
- 加载状态使用 `Spin`、`Skeleton` 或按钮 loading。
- 表单使用 Ant Design `Form`，金额输入前端展示元，提交前转换为分。
- 图表使用当前项目已有图表方案，优先复用已有指标。

## 用户端页面要点

- 首页顶部展示“发现好物”和搜索框，随后是轮播图、分类筛选和商品列表。
- 分类筛选保持紧凑，默认显示一级分类，选中后逐级展开子分类。
- 商品卡片展示封面、名称、店铺、价格、划线价、销量和关键标签。
- 商品详情图片支持切换，不把图片点击误导为浏览器查看图片。
- 评论区位于商品基本信息和图文详情之间，默认展示部分评价。
- 客服弹窗从商品详情或订单详情右下角打开，便于一边查看商品/订单一边咨询。
- AI 助手以右侧悬浮入口出现，不遮挡主要购买流程。

## 管理端页面要点

- 平台后台和商家后台路由、菜单和登录态分离。
- 表格中状态使用中文文案和语义色，不直接展示英文枚举。
- 有搜索、筛选、分页、导出等操作时，布局尽量固定在列表上方和下方。
- 平台视角展示全局数据，商家视角只展示本店数据。
- 报表页面优先保证图表清楚、指标名称准确、空状态友好。

## 文案原则

- 面向用户的页面不出现“测试”“接口返回”“debug”“mock”等开发术语。
- 按钮文案用真实业务动作，例如“去结算”“确认收货”“申请售后”“联系商家客服”。
- 状态文案使用中文，例如“待支付”“待发货”“进行中”“已结束”。
- 错误提示尽量说明用户下一步能做什么。

## 后续优化方向

- 统一图标风格和按钮层级。
- 补齐更多空状态和异常状态。
- 优化表格列宽和移动端适配。
- 优化社区帖子详情、客服消息、订单详情和售后详情的信息层级。
- 为答辩准备一套稳定、干净、有代表性的演示数据。
