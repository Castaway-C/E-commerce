# 一次买够（It's Mygo）实现设计说明书

> 本文基于《需求规格说明书》整理，用于指导后续全量开发。当前项目已经具备电商平台基本功能闭环，后续扩展必须保持后端、前端、接口文档和测试同步推进。

---

## 一、项目定位与实现原则

### 1.1 平台命名

- 中文名：一次买够
- 英文名：It's Mygo
- 项目代码名：`its-mygo`

### 1.2 实现目标

平台是一个社交新零售电商系统，核心链路为：

```text
浏览商品/社区种草 -> 加购/立即购买 -> 结算支付 -> 商家发货 -> 用户收货评价 -> 社区转化与积分奖励
```

实现时优先保障以下目标：

| 优先级 | 目标 | 说明 |
|---|---|---|
| P0 | 主交易链路完整可用 | 注册登录、商品浏览、购物车、下单、支付宝沙箱扫码支付、发货、收货、评价、售后 |
| P0 | 后台可运营 | 商品、订单、用户、社区内容、促销活动、数据看板均可管理 |
| P1 | 社交种草闭环 | 已购用户发布种草帖，其他用户通过帖子购买后给原作者发放积分 |
| P1 | 促销闭环 | 优惠券、满减、积分抵扣、拼团基础链路已接入；限时价和营销标签后续实现 |
| P1 | 库存一致性 | 当前以后端数据库库存校验和事务扣减为主；Redis 预扣与 Lua 防超卖作为后续高并发增强 |
| P2 | 前端最终迭代、复杂报表、客服 | 移交后按完整需求实现，避免只留下不可验收的占位 |

后续开发约定：

- 后端新增功能时，必须同步补用户端或管理端调用入口、页面展示、接口文档和测试。
- 每个模块进入扩展开发时，应按需求完整补齐状态机、权限、异常处理、页面交互和验收路径。
- 当前仅商家入驻需要事前审核。商品、帖子、评论、评价均采用发布后管理；平台保留上下架、隐藏、禁用和操作日志能力。

### 1.3 与需求文档的关键对齐

需求文档要求用户端“以 PC 端为主，适当兼容移动端”。因此实现设计中将用户端定义为响应式 Web，而不是只面向手机的 H5。

后台管理端服务对象是平台运营和商家运营；普通用户不进入后台。需求中的“普通用户”作为系统角色存在于用户端，不作为管理端登录角色处理。

---

## 二、系统总体架构

系统采用前后端分离架构，划分为用户端、管理端和后端服务三部分。

```text
用户端响应式 Web              管理端 Web
React + TypeScript            React + TypeScript
        │                            │
        └──────────────┬─────────────┘
                       │
                    Nginx
          /api/* -> FastAPI HTTP API
          /ws/*  -> FastAPI WebSocket
          /static/uploads/* -> 静态资源
                       │
                 FastAPI 应用
    ┌──────────────────┼──────────────────┐
    │                  │                  │
 Routers           Services          WebSocket
 参数校验/分发      业务逻辑/事务       客服/通知/对时
    │                  │                  │
    └──────────────────┼──────────────────┘
                       │
        ┌──────────────┼──────────────┐
        │              │              │
   MySQL/SQLite      Redis          Celery
   核心持久化       缓存/任务队列     异步任务
   事务/索引        后续库存预扣     超时取消/发券/导出
```

### 2.1 架构边界

| 层级 | 职责 |
|---|---|
| 用户端 | 商品、店铺、社区、购物车、下单、支付、个人中心 |
| 管理端 | 平台运营和商家运营的中后台管理 |
| FastAPI 路由层 | 接收请求、鉴权、参数校验、调用 Service，不写复杂业务 |
| Service 层 | 业务规则、事务边界、跨模块协作 |
| Model/Repository 层 | SQLAlchemy 模型、查询封装、Redis 访问 |
| Celery | 延迟任务、异步任务、报表导出、活动预热 |
| WebSocket | 后续客服聊天、订单通知、限时活动对时，不承担下单扣库存等关键交易逻辑 |

---

## 三、技术栈

### 3.1 用户端响应式 Web

| 分类 | 选型 | 说明 |
|---|---|---|
| 框架 | React + TypeScript | 与需求一致 |
| 构建 | Vite | 启动快，适合实训快速开发 |
| UI | Ant Design 5 + CSS 响应式布局 | PC 优先，同时兼容移动端 |
| 状态管理 | React 状态 + 可选轻量 store | 当前以前端页面状态和 localStorage token 为主，后续正式拆页可引入 Zustand |
| 路由 | React Router v6 | 页面路由 |
| 请求 | Axios | token 注入、错误拦截、刷新 token |
| 图表 | ECharts | 用户端少量可视化可复用 |
| 代码规范 | ESLint + Prettier | 前端统一格式 |

用户端不采用 Ant Design Mobile 作为主 UI 方案，避免与“PC 端为主”的需求冲突。移动端适配通过 CSS media query、弹性布局和少量移动样式完成。

### 3.2 管理端 Web

| 分类 | 选型 | 说明 |
|---|---|---|
| 工程基座 | Vite + React | 当前实际工程基座，后续可按需要引入更多后台框架能力 |
| UI | Ant Design 5 | 表格、表单、详情页效率高 |
| 权限 | RBAC 简化实现 | 后端返回角色、菜单和权限点 |
| 状态管理 | React 状态 + localStorage token | 平台端和商家端 session 分离，后续可引入统一 store |
| 图表 | ECharts | 数据看板 |

### 3.3 后端

| 分类 | 选型 | 说明 |
|---|---|---|
| Web 框架 | FastAPI | 自动 OpenAPI 文档，异步友好 |
| ORM | SQLAlchemy 2.0 async | 成熟稳定 |
| 数据迁移 | Alembic | 管理表结构变更 |
| 数据库 | SQLite 本地联调 / MySQL 8.0 后续联调 | 当前开发可用 SQLite，正式联调建议 MySQL |
| 缓存/锁/队列 | Redis 7.x | Celery Broker、缓存、token 黑名单、后续库存预扣 |
| 异步任务 | Celery | 超时订单取消、自动收货、批量发券、报表导出 |
| 鉴权 | JWT + Bcrypt | access/refresh token |
| 文件处理 | local uploads + 可切换图床 | 先本地，联调时可使用图床 URL |

---

## 四、角色与权限设计

### 4.1 用户体系

| 角色 | 登录端 | 说明 |
|---|---|---|
| consumer | 用户端 | 普通消费者/会员 |
| merchant_operator | 管理端 | 商家运营，只能管理自己店铺的数据 |
| platform_operator | 管理端 | 平台运营，可管理全平台数据、审核商家入驻并进行内容治理 |

普通用户和后台管理员分表管理：

- `user`：消费者账号。
- `admin_user`：后台账号，包含 `role` 和可选 `merchant_id`。
- 商家账号必须绑定 `merchant_id`，所有商品、订单、帖子、客服会话都要按商家隔离。

### 4.2 权限边界

| 功能 | consumer | merchant_operator | platform_operator |
|---|---|---|---|
| 浏览商品/社区 | 是 | 可使用测试用户账号浏览 | 是 |
| 下单/评价/发帖 | 是 | 否 | 否 |
| 商品管理 | 否 | 仅本店商品 | 全平台 |
| 订单发货 | 否 | 仅本店订单 | 可查看全平台，可协助处理 |
| 内容治理 | 否 | 可查看和管理本店广告帖 | 全平台隐藏、下架、禁用 |
| 促销配置 | 否 | 可配置本店活动，平台可限制范围 | 全平台 |
| 数据看板 | 否 | 本店数据 | 全平台数据 |
| 管理员管理 | 否 | 否 | 是 |

---

## 五、模块划分与边界

### 5.1 商品中心 Products

负责商品、SKU、分类、营销标签、店铺展示、商品评价。

| 内容 | 设计 |
|---|---|
| 核心实体 | `merchant`, `product`, `sku`, `category`, `product_image`, `product_tag`, `review` |
| 依赖 | 基础模块，不依赖订单和促销 |
| 被依赖 | 订单、促销、社区、客服 |
| 关键规则 | 商品必须归属商家；SKU 管理价格和库存；评价必须来自已完成订单 |

商品状态：

```text
merchant_operator 创建商品 -> on_sale 在售
on_sale -> off_sale 下架
on_sale -> sold_out 售罄
```

说明：`draft`、`pending_audit`、`audit_rejected` 仅作为旧数据或兼容接口状态保留，不再是新商品发布的主流程。

### 5.2 交易与订单 Orders

负责购物车、结算、订单、支付、发货、收货、退款退货。

| 内容 | 设计 |
|---|---|
| 核心实体 | `cart_item`, `order`, `order_item`, `order_log`, `payment`, `refund`, `shipping_address` |
| 依赖 | 商品、促销、用户积分、社区来源追踪 |
| 关键规则 | 下单时以后端实时价格和库存为准；金额使用分为单位存储 |

订单主状态：

```text
pending_payment 待支付
group_pending 已支付待成团
pending_shipment 待发货
shipping 配送中
pending_receipt 待收货
completed 已完成
cancelled 已取消
after_sale 售后中
closed 已关闭
```

订单主状态转换：

```text
pending_payment --支付成功--> pending_shipment
pending_payment --拼团支付成功--> group_pending
pending_payment --用户取消/支付超时--> cancelled
group_pending --拼团成功--> pending_shipment
group_pending --拼团失败/超时--> closed

pending_shipment --商家发货--> shipping
shipping --系统流转--> pending_receipt
pending_receipt --用户确认/7天自动确认--> completed

pending_receipt --申请售后--> after_sale
completed --申请售后--> after_sale

after_sale --商家拒绝--> 原状态(pending_receipt 或 completed)
after_sale --部分退款完成--> completed
after_sale --全额退款完成--> closed
```

支付状态单独记录：

```text
unpaid -> paid -> refunded/partial_refunded
unpaid -> closed
```

支付与订单关系：

- 普通单商家结算：1 个 `payment` 对应 1 个 `order`。
- 多商家购物车结算：1 个 `payment` 对应多个按商家拆分的 `order`。
- 拼团参团：每个参团用户生成自己的 `payment` 和 `order`，再挂到同一个 `group_buy_order`。
- 通过 `payment_order` 关联表记录 payment 与 order 的对应关系、支付分摊金额和退款金额。

退货退款状态单独记录，避免把订单主状态拉得过长：

```text
pending_approval 待商家审核
approved 商家同意
rejected 商家拒绝
returned 用户已寄回
received 商家已收货
refunded 已退款
closed 已关闭
```

售后记录必须保存 `origin_order_status`，用于拒绝售后或部分退款完成后恢复订单主状态。本项目不实现“用户取消售后”流程。

### 5.3 促销引擎 Promotions

负责优惠券、满减、积分抵扣、限时价和拼团。

| 内容 | 设计 |
|---|---|
| 核心实体 | `coupon_template`, `user_coupon`, `full_discount_activity`, `platform_setting`, `group_buy_activity`, `group_buy_order`, `group_buy_participant`；后续限时价可新增 `flash_activity`, `flash_activity_sku` |
| 依赖 | 商品 SKU、会员等级 |
| 被依赖 | 结算价格计算 |

促销优先级：

```text
普通订单：商品价或限时价 -> 满减 -> 优惠券 -> 积分抵扣 -> 支付宝支付
拼团订单：拼团价 -> 积分抵扣 -> 支付宝支付
```

规则说明：

- 限时特价为后续增强，建议使用普通 SKU 库存，活动价单独记录在 `flash_activity_sku`。
- 若需要防超卖，后续可使用 Redis SKU 库存预扣，不建立独立活动库存。
- 拼团商品可复用商品和 SKU 信息，但下单入口独立，不进入购物车。
- 拼团订单未成团前不发货；超时未成团、退款和积分回退是移交后的重点硬化任务。

优惠券校验规则：

| 规则 | 说明 |
|---|---|
| 适用范围 | `scope_type` 支持 all/platform、merchant、category、product、sku；对应范围 ID 存入关联表 |
| 门槛 | `min_amount_cent` 为最低消费门槛，按优惠券适用范围内商品金额计算 |
| 优惠类型 | `discount_type` 支持 amount 固定金额、percent 折扣 |
| 叠加 | 默认同一订单只能使用 1 张优惠券；优惠券可与满减叠加，后续如需限制必须由活动配置控制 |
| 限时特价 | 默认可与优惠券叠加；若活动设置 `disable_coupon=true`，则不可用券 |
| 拼团价 | 默认不可再使用优惠券和满减，避免价格规则复杂；积分仍视为支付抵扣渠道，可按平台配置上限继续抵扣 |
| 会员限制 | 可配置 `min_user_level`，不满足等级不可领取或使用 |
| 有效期 | 支持固定起止时间；过期后 Celery 将用户券置为 expired |

`user_coupon` 状态机：

```text
unused 未使用 -> used 已使用
unused 未使用 -> expired 已过期
unused 未使用 -> void 作废
used 已使用 -> refunded_rollback 订单退款后返还/回滚
```

### 5.4 社交与内容 Community

负责社区帖子、评论、点赞、话题、种草来源追踪、客服消息。

| 内容 | 设计 |
|---|---|
| 核心实体 | `post`, `post_image`, `post_product`, `comment`, `like`, `topic_tag`, `post_topic_tag`, `cs_conversation`, `cs_message` |
| 依赖 | 用户、商品、订单 |
| 关键规则 | 帖子、评论、评价发布后默认公开，平台进行事后治理；种草帖必须校验已购商品 |

帖子类型：

```text
normal 普通交流贴
grass 种草帖，必须关联已购商品
merchant_ad 商家广告帖，必须由商家后台账号发布或平台代发
```

内容状态：

```text
published 已发布
published -> hidden 下架/隐藏
```

种草积分规则：

- 用户 A 发布种草帖并关联已购商品。
- 用户 B 从帖子点击商品进入详情或结算，订单记录 `source_post_id` 和 `source_user_id`。
- 用户 B 订单完成后，Celery 发放种草积分给用户 A。
- 同一订单只奖励一次；同一用户购买同一商品是否重复奖励由配置控制，默认只奖励首次有效转化。

### 5.5 会员与账户 Users

负责消费者账号、会员等级、积分、签到、地址。

| 内容 | 设计 |
|---|---|
| 核心实体 | `user`, `user_level`, `user_address`, `points_account`, `points_log`, `sign_in_record` |
| 依赖 | 订单完成金额、社区转化 |

会员等级：

```text
normal 注册即有
silver 累计消费满 500 元
gold 累计消费满 2000 元
diamond 累计消费满 5000 元
```

积分流水必须记录业务来源：

```text
sign_in 签到
purchase_reward 购物返利
grass_reward 种草奖励
order_points_deduction 下单支付抵扣
order_points_restore 未支付取消或超时关闭退回
refund_rollback 退款相关回滚
manual_adjust 后台调整
```

积分抵扣设计：

- 积分性质类似账户余额，是支付时金额扣除渠道之一，不作为互斥促销活动。
- 积分可以与优惠券、满减、限时价、拼团等所有优惠叠加。
- 每笔订单可使用积分上限、积分兑换比例、签到奖励、会员等级阈值等必须由平台配置表维护，不得写死在订单服务中。
- 当前配置入口为平台管理端会员与积分规则，后端接口为 `/api/v1/admin/settings/member-points`。
- 下单创建支付单时扣减积分并写 `order_points_deduction` 流水；支付单未支付取消或超时关闭时退回积分并写 `order_points_restore` 流水。

### 5.6 数据与后台管理 Admin

负责后台菜单、管理员账号、数据看板、导出、操作日志。

| 内容 | 设计 |
|---|---|
| 核心实体 | `admin_user`, `admin_menu`, `operation_log` |
| 依赖 | 所有业务模块 |
| 关键规则 | 平台运营看全量，商家运营只看本店 |

---

## 六、核心业务流程

### 6.1 用户下单流程

```text
1. 前端提交选中商品/SKU/数量/地址/优惠券/积分使用量，允许包含多个商家的商品。
2. 后端读取 SKU 最新价格、库存、商品状态和活动状态。
3. Promotion/Price Service 按“商品价或限时价 -> 满减 -> 优惠券 -> 积分抵扣”计算普通订单金额。
4. 按商家拆分订单草稿，生成同一个 payment 预支付单和多个 order。
5. 后端校验 SKU 库存并准备扣减；后续高并发增强可在此加入 Redis Lua 预扣。
6. 数据库事务创建 payment、order、order_item 记录，扣减库存，写订单日志，清理购物车已购买项。
7. 如果事务失败或抛出异常，回滚数据库事务；若后续接入 Redis 预扣，还必须补偿 Redis 库存。
8. 创建 Celery 延迟任务，15 分钟后检查待支付 payment。
9. 前端进入支付页，调用支付宝沙箱扫码支付预创建接口并展示二维码。
10. 支付成功后，同一 payment 下全部订单变为 pending_shipment，通知对应商家后台。
11. 若超时未支付，Celery 关闭 payment，取消其下所有订单并回补库存。
```

实现约定：

- 下单必须幂等，前端传 `client_order_token`，后端防重复提交。
- 价格计算结果必须落库到订单明细，后续商品改价不影响历史订单。
- 订单金额字段统一使用整数分：`total_amount`, `full_discount_amount`, `coupon_discount_amount`, `points_discount_amount`, `pay_amount`；`discount_amount` 可作为总抵扣兼容展示字段。
- `POST /orders` 返回 `payment_id`、`payment_no` 和 `orders[]`，前端以 payment 为单位展示“去支付”。
- 若暂时不实现聚合支付，可在接口层限制一次结算只能包含一个商家；但当前推荐实现聚合支付，减少用户多次付款。

### 6.2 发货与收货流程

```text
pending_shipment -> 商家填写快递公司/快递单号 -> shipping
shipping -> 系统展示配送中，同时进入待收货页面 -> pending_receipt
pending_receipt -> 用户确认收货或发货 7 天后自动确认 -> completed
completed -> 用户可评价商品
```

实训项目不接入真实物流查询，快递状态由商家和用户手动推进即可。

### 6.3 退款退货流程

```text
1. 用户对 completed 或 pending_receipt 订单申请退货退款。
2. 系统生成 refund 记录，保存 origin_order_status，订单主状态变为 after_sale。
3. 商家审核：同意则 refund.approved，拒绝则 refund.rejected，订单恢复原状态。
4. 用户填写退货单号，refund.returned。
5. 商家确认收货，refund.received。
6. 系统执行退款事务：payment 状态变更、库存回滚、必要的积分/优惠权益回滚、refund.refunded。
7. 订单关闭或恢复为 completed，按是否全额退款决定。
```

售后状态处理：

| 场景 | 订单主状态结果 |
|---|---|
| 商家拒绝售后 | 恢复 `origin_order_status` |
| pending_receipt 订单全额退款 | `closed` |
| completed 订单全额退款 | `closed` |
| completed 订单部分退款 | `completed` |
| 多订单聚合支付中单个订单退款 | 只更新该订单和对应 `payment_order` 退款金额，payment 可变为 partial_refunded |

### 6.4 社区种草闭环

```text
1. 用户 A 购买并完成商品。
2. 用户 A 发布 grass 类型帖子，选择已购商品。
3. 后端校验已完成订单，创建帖子，状态为 published。
4. 用户 B 从帖子进入商品并下单，订单记录 source_post_id。
5. 用户 B 订单完成，Celery 发放种草积分给用户 A。
6. WebSocket 或站内通知告知用户 A 积分到账。
```

### 6.5 限时特价流程

```text
1. 平台或商家创建限时活动，配置活动时间、活动 SKU、活动价。
2. 活动开始前 5 分钟，Celery Beat 预热活动 SKU 和库存缓存。
3. 前端进入活动页，通过 WebSocket 获取服务器时间并展示倒计时。
4. 用户下单时后端校验活动是否有效，并按活动价计算。
5. Redis Lua 预扣普通 SKU 库存，MySQL 事务最终扣减。
6. 活动结束后清理活动缓存。
```

### 6.6 拼团流程

```text
1. 用户选择拼团商品并直接发起拼团订单，不进入购物车。
2. 系统创建主订单 order，订单状态为 pending_payment，order_type=group_buy，并创建/关联 group_buy_order。
3. 用户支付成功后，主订单暂不进入 pending_shipment，订单主状态变为 group_pending，group_buy_order 状态为 grouping。
4. 其他用户通过拼团链接参团，各自生成独立 order 和 group_buy_participant。
5. 达到成团人数后，group_buy_order 变为 success，所有已支付参团订单进入 pending_shipment。
6. 超过成团截止时间未满员时，后续应由 Celery 将 group_buy_order 置为 failed；已支付的 group_pending 订单进入退款或关闭流程，未支付订单进入 cancelled，并回补库存和积分。
```

拼团实现约定：

- 主订单状态使用 `group_pending`，表示已支付但等待成团。
- `group_buy_order` 与 `order` 为 1:N：一个团包含多个用户订单。
- `group_buy_participant` 记录 `group_buy_order_id`、`order_id`、`user_id`、`paid_at`、`status`。
- 拼团价不参与普通购物车结算，默认不叠加优惠券和满减；积分仍作为支付抵扣渠道，按平台配置上限参与抵扣。

---

## 七、前后端交互约定

### 7.1 HTTP API 规范

统一前缀：

- 用户端 API：`/api/v1`
- 管理端 API：`/api/v1/admin`

统一响应：

```json
{
  "code": 20000,
  "message": "success",
  "data": {},
  "request_id": "uuid"
}
```

分页响应：

```json
{
  "code": 20000,
  "message": "success",
  "data": {
    "items": [],
    "pagination": {
      "page": 1,
      "page_size": 20,
      "total": 100,
      "total_pages": 5
    }
  },
  "request_id": "uuid"
}
```

错误码：

| HTTP | 业务码 | 含义 |
|---|---|---|
| 200 | 20000 | 成功 |
| 400 | 40001 | 参数校验失败 |
| 400 | 40002 | 业务规则校验失败 |
| 401 | 40100 | 未登录或 token 过期 |
| 401 | 40101 | 登录凭证无效 |
| 403 | 40300 | 无权限 |
| 404 | 40400 | 资源不存在 |
| 409 | 40900 | 资源冲突，如重复下单、库存不足 |
| 422 | 42200 | Pydantic 校验失败 |
| 429 | 42900 | 请求限流 |
| 500 | 50000 | 服务器内部错误 |

分页参数：

```text
page 从 1 开始
page_size 默认 20，最大 100
```

排序参数：

```text
sort_by=price&sort_order=asc
sort_by=sales&sort_order=desc
```

### 7.2 Token 与登录

| Token | 有效期 | 存储位置 | 用途 |
|---|---|---|---|
| access_token | 15 分钟 | 当前前端使用 localStorage；后续可评估 sessionStorage 或内存存储 | 请求鉴权 |
| refresh_token | 7 天 | HttpOnly + Secure + SameSite=Strict Cookie | 刷新 access_token |

刷新流程：

```text
接口返回 401 -> Axios 拦截器携带 Cookie 调用 /auth/refresh -> 获取新 access_token -> 重放原请求
refresh 失败 -> 清除登录态 -> 跳转登录
```

管理端和用户端登录接口分离，避免消费者 token 误用于后台：

- 用户端：`/api/v1/auth/login`
- 管理端：`/api/v1/admin/auth/login`

Cookie 约定：

- refresh_token 不向 JavaScript 暴露，降低 XSS 窃取长期凭证的风险。
- 登出时服务端清除 refresh_token Cookie，并将 access_token 的 jti 写入 Redis 黑名单。
- 如果本地开发未启用 HTTPS，可在开发环境临时关闭 Secure；联调和演示环境必须开启 Secure。

### 7.3 WebSocket

使用 FastAPI 原生 WebSocket。

端点：

| 端点 | 说明 |
|---|---|
| `/ws/chat/{conversation_id}?token=xxx` | 用户与商家客服 |
| `/ws/flash/{activity_id}?token=xxx` | 限时活动服务器时间同步 |
| `/ws/notify?token=xxx` | 用户通知 |
| `/ws/admin/notify?token=xxx` | 后台通知 |

消息格式：

```json
{
  "type": "chat_message",
  "timestamp": "2026-06-25T10:30:00+08:00",
  "payload": {
    "conversation_id": 1,
    "sender_type": "consumer",
    "sender_id": 1001,
    "content": "你好",
    "msg_type": "text"
  }
}
```

约定：

- 客户端每 30 秒发送 `ping`，服务端回复 `pong`。
- 60 秒未收到任何消息则客户端主动重连。
- 重连采用指数退避：1s、2s、4s、8s，最大 30s。

### 7.4 文件上传

接口：

```text
POST /api/v1/upload
Content-Type: multipart/form-data
```

响应：

```json
{
  "code": 20000,
  "message": "success",
  "data": {
    "url": "/static/uploads/images/2026/06/25/xxx.jpg",
    "thumb_url": "/static/uploads/images/2026/06/25/xxx_thumb.jpg",
    "size": 204800,
    "mime_type": "image/jpeg"
  }
}
```

限制：

- 图片：jpg、jpeg、png、webp、gif，最大 5MB。
- 文档：pdf、docx、xlsx，最大 20MB。
- 服务端必须校验 MIME type 和扩展名。
- 商品图、帖子图、评价图、聊天图片都复用该上传接口。

---

## 八、数据库设计

### 8.1 通用字段

核心业务表建议统一包含：

```text
id BIGINT UNSIGNED PRIMARY KEY AUTO_INCREMENT
created_at DATETIME
updated_at DATETIME
is_deleted TINYINT DEFAULT 0
```

金额字段统一用整数分，避免浮点精度问题：

```text
price_cent
total_amount_cent
discount_amount_cent
pay_amount_cent
```

时间统一存数据库本地时间或 UTC 均可，但接口返回必须带时区。推荐后端统一使用 `Asia/Shanghai` 展示。

### 8.2 核心实体关系

```text
Merchant 1:N Product
Merchant 1:N AdminUser
Merchant 1:N CsConversation

Product 1:N Sku
Product N:1 Category
Product 1:N ProductImage
Product N:M ProductTag
Product 1:N Review

User 1:N UserAddress
User 1:1 PointsAccount
User 1:N PointsLog
User 1:N SignInRecord
User 1:N Post

Post 1:N PostImage
Post 1:N Comment
Post N:M Product
Post N:M TopicTag
Post 1:N Like

Payment N:M Order (关联表 payment_order)
Order 1:N OrderItem
Order 1:N OrderLog
Order 0:N Refund
OrderItem N:1 Sku

CouponTemplate 1:N UserCoupon
FlashActivity 1:N FlashActivitySku
FullDiscountActivity 1:N FullDiscountRule
GroupBuyActivity 1:N GroupBuyOrder
GroupBuyOrder 1:N GroupBuyParticipant
GroupBuyOrder 1:N Order
GroupBuyParticipant N:1 Order
```

### 8.3 关键表补充

#### user

| 字段 | 说明 |
|---|---|
| mobile | 手机号，登录账号 |
| nickname | 昵称 |
| avatar_url | 头像 |
| gender | 性别，可为空 |
| birthday | 生日，可为空 |
| email | 邮箱，可为空 |
| level | 会员等级 |
| points | 当前积分余额 |

#### user_address

| 字段 | 说明 |
|---|---|
| user_id | 所属用户 |
| receiver_name | 收货人 |
| receiver_mobile | 收货手机号 |
| province/city/district/street | 省、市、区县、街道 |
| detail_address | 详细地址 |
| postal_code | 邮编，可为空 |
| address_tag | 地址标签，如家、公司，可为空 |
| is_default | 是否默认地址 |

#### merchant

| 字段 | 说明 |
|---|---|
| id | 商家 ID |
| name | 店铺名称 |
| logo_url | 店铺 Logo |
| notice | 店铺公告 |
| status | normal/disabled |

#### product

| 字段 | 说明 |
|---|---|
| merchant_id | 所属商家 |
| category_id | 分类 |
| name | 商品名称 |
| description | 富文本详情 |
| status | on_sale/off_sale/sold_out；draft/pending_audit/audit_rejected 仅兼容旧数据 |
| sales_count | 销量 |

#### sku

| 字段 | 说明 |
|---|---|
| product_id | 商品 |
| sku_name | 规格组合名 |
| attrs_json | 规格属性，如颜色、尺码 |
| price_cent | 原价/日常价 |
| stock | 库存 |
| version | 乐观锁版本 |

#### order

| 字段 | 说明 |
|---|---|
| order_no | 唯一订单号 |
| user_id | 下单用户 |
| merchant_id | 订单所属商家 |
| status | 订单主状态 |
| pay_status | 支付状态 |
| total_amount_cent | 商品总额 |
| discount_amount_cent | 优惠金额 |
| points_deduction_cent | 积分抵扣金额 |
| pay_amount_cent | 实付金额 |
| source_post_id | 来源种草帖，可为空 |
| source_user_id | 来源用户，可为空 |

多商家购物车结算时，按商家拆分为多个订单，并通过同一个 `payment` 聚合支付。

#### payment

| 字段 | 说明 |
|---|---|
| payment_no | 唯一支付单号 |
| user_id | 支付用户 |
| pay_amount_cent | 本次应付金额 |
| status | unpaid/paid/partial_refunded/refunded/closed |
| pay_channel | alipay；mock 仅作为历史兼容或后端测试兜底，不作为正常前端业务路径 |
| paid_at | 支付成功时间 |

#### payment_order

| 字段 | 说明 |
|---|---|
| payment_id | 支付单 |
| order_id | 订单 |
| amount_cent | 该订单分摊支付金额 |
| refunded_amount_cent | 该订单已退款金额 |

#### group_buy_order

| 字段 | 说明 |
|---|---|
| activity_id | 拼团活动 |
| leader_user_id | 团长 |
| status | grouping/success/failed/cancelled |
| required_count | 成团人数 |
| current_count | 已支付参团人数 |
| expire_at | 成团截止时间 |

#### group_buy_participant

| 字段 | 说明 |
|---|---|
| group_buy_order_id | 拼团单 |
| order_id | 参团用户对应的主订单 |
| user_id | 参团用户 |
| status | pending_payment/paid/cancelled/refunded |
| paid_at | 支付时间 |

### 8.4 索引建议

| 表 | 索引 |
|---|---|
| product | `merchant_id`, `category_id`, `status, created_at`, `name` |
| sku | `product_id`, `price_cent`, `stock` |
| order | `order_no unique`, `user_id, status`, `merchant_id, status`, `created_at` |
| order_item | `order_id`, `sku_id` |
| post | `user_id`, `status, created_at`, `type` |
| comment | `post_id`, `status` |
| user_coupon | `user_id, status`, `coupon_template_id` |
| points_log | `user_id, created_at` |
| admin_user | `username unique`, `merchant_id` |

### 8.5 删除策略

| 数据 | 策略 |
|---|---|
| 商品、SKU、帖子、评论、用户 | 软删除 |
| 订单、支付、积分流水、操作日志 | 不删除 |
| 优惠券、活动 | 状态停用 |

---

## 九、缓存、库存与异步任务

### 9.1 Redis 使用

| Key | 类型 | 用途 |
|---|---|---|
| `cart:{user_id}` | Hash | 购物车，field 为 sku_id，value 为数量 |
| `stock:sku:{sku_id}` | String | SKU 库存缓存 |
| `lock:order:{order_no}` | String NX EX | 下单幂等/短锁 |
| `coupon:{coupon_id}:claimed` | Set | 领券去重 |
| `flash:{activity_id}` | Hash/ZSet | 限时活动缓存 |
| `jwt:blacklist:{jti}` | String | 登出 token 黑名单，TTL 等于 token 剩余有效期 |

### 9.2 库存扣减策略

```text
读取 Redis 库存 -> Lua 原子预扣 -> MySQL 事务扣减 -> 成功提交
                                      └─ 失败/异常 -> 立即补偿 Redis
```

Lua 示例：

```lua
local stock = redis.call('GET', KEYS[1])
if not stock or tonumber(stock) < tonumber(ARGV[1]) then
  return 0
end
redis.call('DECRBY', KEYS[1], ARGV[1])
return 1
```

MySQL 扣减建议使用乐观锁：

```sql
UPDATE sku
SET stock = stock - :quantity, version = version + 1
WHERE id = :sku_id AND stock >= :quantity AND version = :version;
```

补偿规则：

- Redis 预扣成功后，必须在内存中记录 `reserved_items = [{sku_id, quantity}]`。
- MySQL 事务提交失败、乐观锁扣减失败、订单创建异常或进程捕获业务异常时，立即遍历 `reserved_items` 执行 `INCRBY stock:sku:{sku_id} quantity`。
- 补偿成功与失败都要写日志；补偿失败时写入 `stock_compensation_log`，由定时任务重试。
- Celery 超时取消只处理“订单已创建但未支付”的场景，不替代 MySQL 事务失败时的即时补偿。
- 增加 `reconcile_stock_cache` 定时对账任务，每天或每小时扫描热点 SKU，对比 Redis 库存与 MySQL `sku.stock`，发现差异后按订单预占记录修正。

### 9.3 Celery 任务

| 任务 | 触发 | 延迟 | 说明 |
|---|---|---|---|
| `check_payment_timeout` | 创建订单 | 15 分钟 | 未支付则取消订单、回滚库存 |
| `auto_confirm_receipt` | 商家发货 | 7 天 | 自动确认收货 |
| `reward_grass_points` | 订单完成 | 立即 | 发放种草积分 |
| `expire_coupons` | 定时 | 每天 | 过期优惠券作废 |
| `batch_send_coupons` | 运营触发 | 立即 | 批量发券 |
| `warm_flash_activity` | 定时 | 活动前 5 分钟 | 预热活动缓存 |
| `clean_flash_activity` | 定时 | 活动结束后 | 清理活动缓存 |
| `check_group_buy_timeout` | 拼团创建 | 到期时 | 未成团退款 |
| `reconcile_stock_cache` | 定时 | 每小时/每天 | Redis 与 MySQL 库存对账，修复异常预占 |
| `export_data` | 后台请求 | 立即 | 生成 Excel |

所有任务必须具备幂等性，例如通过业务唯一键或状态判断避免重复处理。

---

## 十、项目目录结构

```text
its-mygo/
├── user-frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── home/
│   │   │   ├── product/
│   │   │   ├── merchant/
│   │   │   ├── cart/
│   │   │   ├── checkout/
│   │   │   ├── order/
│   │   │   ├── community/
│   │   │   ├── promotion/
│   │   │   ├── user/
│   │   │   └── auth/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── services/
│   │   ├── store/
│   │   ├── routes/
│   │   ├── types/
│   │   └── utils/
│   ├── vite.config.ts
│   └── package.json
│
├── admin-frontend/
│   ├── src/
│   │   ├── pages/
│   │   │   ├── dashboard/
│   │   │   ├── product/
│   │   │   ├── order/
│   │   │   ├── promotion/
│   │   │   ├── community/
│   │   │   ├── user/
│   │   │   ├── merchant/
│   │   │   ├── system/
│   │   │   └── auth/
│   │   ├── components/
│   │   ├── layouts/
│   │   ├── services/
│   │   ├── store/
│   │   └── routes/
│   └── package.json
│
├── backend/
│   ├── app/
│   │   ├── api/v1/
│   │   ├── core/
│   │   ├── db/
│   │   ├── models/
│   │   ├── schemas/
│   │   ├── services/
│   │   ├── tasks/
│   │   ├── websocket/
│   │   └── utils/
│   ├── alembic/
│   ├── tests/
│   ├── uploads/
│   ├── requirements.txt
│   └── docker-compose.yml
│
├── docs/
├── scripts/
├── .env.example
├── .gitignore
└── README.md
```

---

## 十一、主要页面设计

### 11.1 用户端页面

| 页面 | 核心内容 |
|---|---|
| 首页 | Banner、分类入口、爆款推荐、猜你喜欢、限时特价入口 |
| 商品列表 | 搜索、分类、价格筛选、排序 |
| 商品详情 | 图片轮播、SKU 选择、价格库存、评价、店铺入口、客服、加购/购买 |
| 店铺页 | 店铺信息、公告、在售商品、商家广告帖 |
| 购物车 | SKU 列表、失效商品提示、批量选择、结算 |
| 结算页 | 地址、商品、优惠券、积分、金额明细、提交订单 |
| 订单列表/详情 | 状态筛选、支付、取消、确认收货、申请退货、评价 |
| 社区广场 | 最新/热度排序、标签筛选、发帖 |
| 帖子详情 | 内容、关联商品、点赞、评论、购买入口 |
| 个人中心 | 资料、等级、积分、优惠券、地址、帖子、订单 |

### 11.2 管理端页面

| 页面 | 平台运营 | 商家运营 |
|---|---|---|
| 数据看板 | 全平台数据 | 本店数据 |
| 商品管理 | 全量监管/上下架 | 本店商品增删改查 |
| 订单管理 | 全量查看/协助处理 | 本店发货/退款处理 |
| 促销管理 | 全平台活动 | 本店活动 |
| 社区管理 | 内容治理/下架 | 本店广告帖管理 |
| 用户管理 | 用户查看/禁用 | 不可用或只读购买本店用户 |
| 商家管理 | 商家资料和账号 | 本店资料维护 |
| 系统管理 | 管理员、日志、导出 | 仅本店日志/导出 |

---

## 十二、主要 API 清单

### 12.1 用户端 API

#### 认证与用户

| 方法 | URL | 说明 |
|---|---|---|
| POST | `/auth/register` | 注册 |
| POST | `/auth/login` | 登录 |
| POST | `/auth/refresh` | 刷新 token |
| POST | `/auth/logout` | 登出 |
| GET | `/users/profile` | 当前用户资料 |
| PUT | `/users/profile` | 编辑资料 |
| GET | `/users/level` | 会员等级 |
| GET | `/users/points` | 积分账户 |
| GET | `/users/points/logs` | 积分流水 |
| POST | `/users/sign-in` | 签到 |
| GET | `/addresses` | 地址列表 |
| POST | `/addresses` | 新增地址 |
| PUT | `/addresses/{id}` | 编辑地址 |
| DELETE | `/addresses/{id}` | 删除地址 |

#### 商品与店铺

| 方法 | URL | 说明 |
|---|---|---|
| GET | `/products` | 商品列表/搜索 |
| GET | `/products/{id}` | 商品详情 |
| GET | `/products/{id}/reviews` | 商品评价 |
| GET | `/products/{id}/similar` | 相关推荐 |
| GET | `/categories` | 分类树 |
| GET | `/merchants/{id}` | 店铺主页 |
| GET | `/merchants/{id}/products` | 店铺商品 |
| GET | `/merchants/{id}/posts` | 店铺广告帖 |

分类实现约定：

- `/categories` 返回启用分类的扁平列表，字段包含 `id`、`name`、`parent_id`、`sort_order`，前端按 `parent_id` 组装树。
- 分类最多三级；`parent_id = null` 表示一级分类。
- `sort_order` 只表示同一父级下展示顺序，数字越小越靠前。
- `/products` 和管理端商品列表传入父级 `category_id` 时，应包含该分类及全部子孙分类下的商品。

商品详情实现约定：

- 商品支持多张图片，创建/编辑商品时通过 `image_urls` 保存到 `product_image`。
- `cover_url` 作为列表和详情首图；未单独指定时默认取第一张商品图。
- 当前实训版不引入富文本编辑器，商品图文详情采用 `description` 多行文本 + `images` 多图画廊展示。

#### 购物车与订单

| 方法 | URL | 说明 |
|---|---|---|
| GET | `/cart` | 购物车 |
| POST | `/cart` | 加入购物车 |
| PUT | `/cart/{sku_id}` | 修改数量 |
| DELETE | `/cart/{sku_id}` | 删除 |
| POST | `/cart/checkout` | 结算预校验 |
| POST | `/orders` | 创建订单 |
| GET | `/orders` | 订单列表 |
| GET | `/orders/{id}` | 订单详情 |
| POST | `/orders/{id}/cancel` | 取消订单 |
| GET | `/payments/{id}` | 支付单详情，包含关联订单 |
| POST | `/payments/{id}/alipay/precreate` | 支付宝沙箱扫码支付预创建 |
| POST | `/payments/{id}/alipay/sync` | 同步支付宝沙箱支付状态 |
| POST | `/orders/{id}/confirm` | 确认收货 |
| POST | `/orders/{id}/reviews` | 发表评价 |
| POST | `/orders/{id}/refunds` | 申请退货退款 |

#### 促销

| 方法 | URL | 说明 |
|---|---|---|
| GET | `/promotions/coupons` | 可领优惠券 |
| POST | `/promotions/coupons/{id}/claim` | 领券 |
| GET | `/promotions/my-coupons` | 我的优惠券 |
| GET | `/promotions/full-discounts/active` | 当前满减活动 |
| GET | `/promotions/flash` | 限时特价列表，后续增强 |
| GET | `/promotions/flash/{id}` | 限时活动详情，后续增强 |
| GET | `/group-buy/activities` | 拼团活动列表 |
| POST | `/group-buy/activities/{activity_id}/start` | 发起拼团 |
| POST | `/group-buy/groups/{group_order_id}/join` | 参与拼团 |

#### 社区

| 方法 | URL | 说明 |
|---|---|---|
| GET | `/community/posts` | 帖子列表 |
| GET | `/community/posts/{id}` | 帖子详情 |
| POST | `/community/posts` | 发布帖子 |
| DELETE | `/community/posts/{id}` | 删除自己的帖子 |
| POST | `/community/posts/{id}/like` | 点赞/取消点赞 |
| GET | `/community/posts/{id}/comments` | 评论列表 |
| POST | `/community/posts/{id}/comments` | 发表评论 |
| DELETE | `/community/comments/{id}` | 删除自己的评论 |
| GET | `/community/tags` | 话题标签 |
| GET | `/community/users/{id}` | 用户主页 |

### 12.2 管理端 API

所有路径以 `/api/v1/admin` 为前缀。

#### 认证与菜单

| 方法 | URL | 说明 |
|---|---|---|
| POST | `/auth/login` | 管理端登录 |
| POST | `/auth/refresh` | 刷新 token |
| POST | `/auth/logout` | 登出 |
| GET | `/auth/me` | 当前管理员 |
| GET | `/auth/menus` | 菜单树和权限点 |

#### 商品与店铺

| 方法 | URL | 说明 |
|---|---|---|
| GET | `/products` | 商品列表 |
| POST | `/products` | 商家创建本店商品，平台不可创建商品 |
| GET | `/products/{id}` | 商品详情 |
| PUT | `/products/{id}` | 编辑商品 |
| POST | `/products/{id}/submit-audit` | 兼容旧接口，当前保持/变为上架 |
| POST | `/products/{id}/audit` | 兼容旧接口，通过为上架，拒绝为下架 |
| POST | `/products/{id}/publish` | 上架 |
| POST | `/products/{id}/unpublish` | 下架 |
| GET | `/categories` | 分类列表 |
| POST | `/categories` | 创建分类，最多三级，父级必须存在且启用 |
| PUT | `/categories/{id}` | 编辑分类 |
| DELETE | `/categories/{id}` | 删除分类 |
| GET | `/reviews` | 评价列表 |
| POST | `/reviews/{id}/audit` | 兼容旧接口，通过为公开，拒绝为隐藏 |
| GET | `/merchants` | 商家列表，平台可用 |
| GET | `/merchant/profile` | 商家查看本店资料 |
| PUT | `/merchant/profile` | 商家编辑本店资料 |

#### 订单与售后

| 方法 | URL | 说明 |
|---|---|---|
| GET | `/orders` | 订单列表 |
| GET | `/orders/{id}` | 订单详情 |
| POST | `/orders/{id}/ship` | 商家发货 |
| GET | `/refunds` | 售后列表 |
| POST | `/refunds/{id}/approve` | 同意售后 |
| POST | `/refunds/{id}/reject` | 拒绝售后 |
| POST | `/refunds/{id}/receive` | 确认收到退货 |
| POST | `/refunds/{id}/refund` | 执行退款 |

#### 促销

| 方法 | URL | 说明 |
|---|---|---|
| GET | `/promotions/coupons` | 优惠券模板 |
| POST | `/promotions/coupons` | 创建优惠券 |
| PUT | `/promotions/coupons/{id}` | 编辑优惠券 |
| POST | `/promotions/coupons/{id}/disable` | 停用 |
| GET | `/promotions/full-discounts` | 满减活动 |
| POST | `/promotions/full-discounts` | 创建满减 |
| GET | `/promotions/flash` | 限时活动 |
| POST | `/promotions/flash` | 创建限时活动 |
| PUT | `/promotions/flash/{id}` | 编辑限时活动 |
| POST | `/promotions/flash/{id}/stop` | 结束限时活动 |
| GET | `/promotions/group-buy` | 拼团活动 |
| POST | `/promotions/group-buy` | 创建拼团活动 |

#### 社区与内容治理

| 方法 | URL | 说明 |
|---|---|---|
| GET | `/community/posts` | 帖子列表 |
| POST | `/community/posts/{id}/audit` | 兼容旧接口，通过为公开，拒绝为隐藏 |
| POST | `/community/posts/{id}/hide` | 下架帖子 |
| GET | `/community/comments` | 评论列表 |
| POST | `/community/comments/{id}/audit` | 兼容旧接口，通过为公开，拒绝为隐藏 |
| POST | `/community/comments/{id}/hide` | 隐藏评论 |
| GET | `/community/tags` | 标签列表 |
| POST | `/community/tags` | 创建标签 |

#### 用户、看板与系统

| 方法 | URL | 说明 |
|---|---|---|
| GET | `/users` | 用户列表 |
| GET | `/users/{id}` | 用户详情 |
| POST | `/users/{id}/ban` | 禁用用户 |
| GET | `/dashboard/summary` | 核心指标 |
| GET | `/dashboard/trend` | 销售趋势 |
| GET | `/dashboard/sales-top` | 商品销量 TOP10 |
| GET | `/dashboard/order-status` | 订单状态分布 |
| GET | `/dashboard/user-growth` | 用户增长 |
| GET | `/system/admin-users` | 管理员列表 |
| POST | `/system/admin-users` | 创建管理员 |
| PUT | `/system/admin-users/{id}` | 编辑管理员 |
| GET | `/system/logs` | 操作日志 |
| GET | `/system/data-export/orders` | 导出订单 |
| GET | `/system/data-export/users` | 导出用户 |

---

## 十三、非功能设计

### 13.1 安全

| 项 | 方案 |
|---|---|
| 密码 | Bcrypt 加盐哈希 |
| 鉴权 | JWT HS256，区分用户端和管理端 |
| SQL 注入 | SQLAlchemy 参数绑定 |
| XSS | 后端过滤富文本危险标签，前端渲染转义 |
| CSRF | 业务 API 使用 Bearer access_token；refresh 接口使用 SameSite=Strict Cookie，必要时增加 CSRF token |
| 限流 | Redis 滑动窗口，登录接口 5 次/分钟，普通接口 60 次/分钟 |
| 文件上传 | MIME 校验、大小限制、随机文件名 |

### 13.2 日志与审计

- 所有请求生成 `request_id`。
- 管理端关键操作写入 `operation_log`。
- 订单状态变化写入 `order_log`。
- 异步任务记录任务 ID、重试次数、错误原因。

### 13.3 测试建议

P0 测试范围：

- 注册、登录、刷新 token。
- 商品列表、商品详情、SKU 选择。
- 加购、结算、下单、支付、超时取消。
- 商家发货、用户确认收货、评价。
- 发布种草帖、通过帖子下单、积分奖励。
- 优惠券、满减、积分抵扣已参与价格计算；限时价后续接入时需补充对应测试。
- 商家账号只能访问本店数据。

---

## 十四、编码顺序建议

推荐按以下顺序让 AI 或小组成员实现：

1. 后端基础设施：配置、数据库、Redis、JWT、统一响应、异常处理。
2. 用户和后台账号：注册登录、角色权限、菜单。
3. 商家、分类、商品、SKU、上传。
4. 用户端商品浏览、店铺页、购物车。
5. 订单、支付宝沙箱扫码支付、数据库库存扣减、Celery 超时取消。
6. 管理端商品、订单、发货。
7. 评价、社区帖子、评论、内容治理。
8. 种草来源追踪和积分奖励完整化。
9. 优惠券、满减、积分抵扣、会员配置、限时价。
10. 拼团、前端最终迭代、数据看板、Excel 导出、客服与通知。

---

## 十五、设计决策记录

| 决策项 | 最终选择 | 原因 |
|---|---|---|
| 用户端形态 | PC 优先响应式 Web | 与需求规格一致 |
| 用户端 UI | Ant Design 5 | PC 体验更完整，并与后台风格统一 |
| 后台角色 | platform_operator / merchant_operator | 普通用户不进入后台 |
| 商家模型 | 独立 `merchant` 表 | 支持店铺页、商家隔离、订单归属 |
| 金额存储 | 整数分 | 避免浮点误差 |
| 库存 | 当前数据库事务扣减，后续可接 Redis 预扣 | 先保证实训闭环，后续按高并发要求增强 |
| 限时特价库存 | 复用普通 SKU 库存 | 符合需求“无需独立库存” |
| 订单和售后状态 | 主状态与退款状态分离 | 避免状态机过度复杂 |
| WebSocket | FastAPI 原生 WebSocket | 满足客服、通知、倒计时即可 |
| Celery Broker | Redis | 减少中间件数量，适合实训 |
