# 测试目录

本目录用于放跨端验收测试、接口联调测试和端到端测试说明。

- 后端单元测试优先放在 `backend/tests/`
- 前端组件测试后续可分别放在 `user-frontend/src/**/*.test.tsx` 与 `admin-frontend/src/**/*.test.tsx`
- 跨端场景测试可以按业务链路建立子目录，例如 `tests/order-flow/`

